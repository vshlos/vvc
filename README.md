This is a very simple MVC framework which requires no model. Why create a model for MVC when javascript handles objects so well already. This of course leaves some of the validation to be done by the other pieces.

The idea of this library is to help with coupling pieces of a website together.

***Still on the building block***